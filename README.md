# snake_challenge_2026

## TODO
- Использовать дружественных змей как "трамплины" — змея может встать на тело союзника, чтобы добраться до батарей, до которых не хватает длины/высоты
- **AER trapped threshold**: сменить `additionalEscape < (ESCAPE_ROUTES + 1)` на `<=` — AER равный порогу рядом с врагом должен считаться ловушкой
- **Body blocking**: учить змей блокировать телом пути врага к батареям — area denial
- **Explorer gravity awareness**: ExplorationScorer должен учитывать опору — предпочитать направления с поддержкой, избегать неконтролируемых падений
- **Chosen target selection**: при выборе цели избранного учитывать дистанцию до противника
- **DFS учёт союзников**: в DFS pathfinding учитывать движение дружественных змей (их тела сдвинутся)
- **DFS учёт гравитации**: корректный учёт гравитации в DFS pathfinding
- **Змея может выходить за границу карты** (правила: "It is possible to extend your snakebot beyond the borders of the grid"). Смерть только при падении за карту, а не при выходе головы за границу. В коде 3 места блокируют OOB-позиции: (1) `getOutOfBoundsPenalty` (строка ~481) штрафует -1000 за OOB, (2) BFS Phase 1 (строка ~711) `isOutsideMap` → continue, (3) BFS Phase 2 (строка ~756) то же. Фикс: убрать OOB-штраф из Safety, разрешить BFS проходить через OOB-клетки (проверять смерть только при падении за нижнюю границу)
